import { ChangePasswordValidatorDirective } from './change-password-validator.directive';

describe('ChangePasswordValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangePasswordValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
